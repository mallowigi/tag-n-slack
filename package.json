{
    "name": "Slackalaka",
    "description": "Creates a new Tag with a Changelog from `CHANGELOG.md` using the `version` from `package.json` and notifies Slack about the release.",
    "version": "0.0.2",
    "scripts": {
        "build": "ncc build src/index.js -o action"
    },
    "dependencies": {
        "@actions/core": "^3.0.0",
        "@actions/github": "^9.0.0",
        "@slack/webhook": "^7.0.7",
        "@tryfabric/mack": "^1.2.1",
        "axios": "^1.13.5",
        "gh-release": "^7.0.2",
        "lodash": "^4.17.23"
    },
    "devDependencies": {
        "@vercel/ncc": "^0.38.4",
        "prettier": "^3.8.1"
    }
}
